/** @type {import('next').NextConfig} */
const nextConfig = {
  // node:sqlite is a built-in Node.js module (available since Node 22.5+)
  // No special configuration needed - it's a native Node.js module
}

module.exports = nextConfig
